version: "3.9"

networks:
  event_network:
    driver: bridge

services:
  database:
    image: postgres
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    networks:
      - event_network